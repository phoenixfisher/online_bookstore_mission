# Online Bookstore

ASP.NET Core backend with a React frontend.

## Run

Backend:

```bash
cd backend/backend
dotnet run
```

Frontend:

```bash
cd frontend
npm install
npm run dev
```

## Bootstrap

Extra Bootstrap features used:
- `sticky-top`
- `badge rounded-pill text-bg-*`
